[project]
name = "signal-service"
version = "0.1.0"
description = "Multi-timeframe confluence scoring and signal emission"
requires-python = ">=3.12"
dependencies = [
    "vix-core",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
    "redis>=5.0,<6",
]

[tool.uv]
package = false

[tool.uv.sources]
vix-core = { workspace = true }
