[project]
name = "risk-service"
version = "0.1.0"
description = "Position sizing, exposure tracking, pre-trade validation"
requires-python = ">=3.12"
dependencies = [
    "vix-core",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false

[tool.uv.sources]
vix-core = { workspace = true }
